import React from "react";
import CreateMealForm from "../components/CreateMealForm";

const CreateMeal = ({ token }) => {
  return (
    <div>
      <CreateMealForm />
    </div>
  );
};

export default CreateMeal;
